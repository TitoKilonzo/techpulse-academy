# Fail install if any package has a known vulnerability at high or critical level
# Remove this if you need to unblock CI — but fix the vulns instead.
# audit-level=high

# Avoid hoisting issues with native modules
prefer-dedupe=true
